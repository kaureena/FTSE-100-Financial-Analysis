# Config folder

This folder holds run-time configuration used by the V2 dashboards/pipeline:
- `thresholds.yaml` — KPI thresholds and alert bands

Rules:
- Any change to thresholds must be documented in V2-P22 Release Notes.
- Keep config values stable to avoid changing historical exports unexpectedly.
