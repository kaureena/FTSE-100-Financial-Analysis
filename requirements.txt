# Core analytics
pandas>=2.0
numpy>=1.24

# Visuals
matplotlib>=3.7

# Forecasting
statsmodels>=0.14
scikit-learn>=1.3
torch>=2.0

# Storage (Parquet)
pyarrow>=14.0

# Notebooks
jupyter>=1.0
nbformat>=5.9

# Optional live data pulls (providers)
requests>=2.31

# Optional reference refresh (Wikipedia constituents scrape)
beautifulsoup4>=4.12
lxml>=5.0

# Optional enrichment (earnings / market caps)
yfinance>=0.2

# Ops / local warehouse
duckdb>=0.10.2
PyYAML>=6.0

Pillow>=10.0

# QA (optional but used in CI)
pytest>=7.4
